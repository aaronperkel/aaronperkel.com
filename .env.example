# Resend API key for the /services inquiry form (app/api/inquiry/route.ts).
# Create one at https://resend.com/api-keys, then add it to the Vercel project
# for Production and Preview. The form is the only thing on the site that needs it.
RESEND_API_KEY=
